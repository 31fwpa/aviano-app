# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**

# AI assistant / editor tooling settings (machine-local, not app code)
.claude/

# Raw export folders dropped in from Lovable. Merge them into the app, then
# delete or archive them OUTSIDE the repo — never commit a second copy of the
# whole project. See MAINTAINER_GUIDE.md §10.
App Changes/
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
